// Deck store keyed by topic slug. The operating-systems deck is the
// full 20-card placeholder from placeholderDeck.js. The other topics
// carry short stand-in decks (3 concept cards, each followed by a quiz
// card, per SKILL_quiz.md) so every topic in the picker leads to a
// working feed; the content pipeline (SKILL.md) will replace these with
// reviewed, difficulty-ordered decks. No em dashes or emojis anywhere.
// card_id is a stable mock id so quiz answers and the end-of-deck score
// work without a backend.

import { placeholderDeck } from "./placeholderDeck";

const computerNetworks = [
  {
    card_id: 201,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "A packet is the unit of the network",
    body: "The network moves data in packets, not streams. A packet is a block of bytes with a header and a payload. The header carries control information, the destination address, the source address, and fields that describe the payload that follows. When an application sends a message larger than one packet, the protocol stack splits it across several packets, each with a sequence number so the receiver can reassemble them in order. Routers inspect only the header of each packet and forward it toward the destination, which is what lets the network work without any single machine knowing the whole path. This unit-based design is why a network can carry many conversations at once over shared links. The cost is that packets can arrive out of order, be duplicated, or be dropped, which is why higher layers add sequence numbers and retransmission.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 202,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "Besides the payload, what does a packet carry?",
    options: [
      { id: "a", text: "A header with destination and source" },
      { id: "b", text: "Only the application data" },
      { id: "c", text: "A copy of the whole conversation" },
      { id: "d", text: "The route through every router" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 203,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "TCP handshake opens a connection",
    body: "TCP turns an unreliable packet network into a reliable byte stream, and it starts with a three-way handshake. The client sends a SYN segment announcing a starting sequence number. The server replies with a SYN-ACK, acknowledging the client number and announcing its own. The client sends an ACK back, and from that point both sides know the initial sequence numbers for both directions. Every byte is numbered, so the receiver can detect gaps, ask for retransmission, and reorder what arrives out of order. The handshake also lets each side allocate buffers before any payload flows, so no data is lost to a receiver that was not ready. This is why opening a TCP connection costs a round trip before the first byte, and why connection reuse matters for latency in real applications.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 204,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "What does the three-way handshake establish?",
    options: [
      { id: "a", text: "Initial sequence numbers for both sides" },
      { id: "b", text: "The length of the network path" },
      { id: "c", text: "The address of every router" },
      { id: "d", text: "The server's process id" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 205,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "DNS turns names into addresses",
    body: "Humans remember names, the network works with numbers, and the Domain Name System bridges the two. A client asks a resolver for the address behind a name such as example.com. If the answer is not cached, the resolver walks a hierarchy: a root server points to the right top-level domain server, which points to the authoritative server for the domain, which returns the address records. The result is cached with a time to live, so repeated lookups for the same name are cheap and do not hit the hierarchy again. Caching is what makes DNS fast in practice, and it is also why a change to a domain's records takes time to propagate everywhere. Because the query is short and the cache is hot, DNS typically resolves in milliseconds, well below what a user would notice.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 206,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "Why is a repeated DNS lookup so fast?",
    options: [
      { id: "a", text: "The answer is cached with a time to live" },
      { id: "b", text: "It always walks the full hierarchy" },
      { id: "c", text: "The root server knows every address" },
      { id: "d", text: "The name is stored in the packet" },
    ],
    correct_option_id: "a",
  },
];

const dataStructures = [
  {
    card_id: 301,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "An array indexes memory directly",
    body: "An array is a block of contiguous memory where every element is the same size, so the address of element i is base plus i times the element size. That arithmetic is why array access is constant time: the CPU computes the address and reads it, no search involved. The price is that inserting or deleting in the middle requires shifting every later element to keep the block contiguous, which costs linear time. The same contiguity makes arrays cache-friendly, since neighboring elements live in nearby memory and are fetched together by the cache. Choosing an array means committing to fixed-size, indexable storage where reads are the fast operation. The alternative shapes, linked structures and hash tables, trade that direct addressing for cheaper inserts or different lookup behavior, and each choice fits a different workload.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 302,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "Why is array access constant time?",
    options: [
      { id: "a", text: "Address is base plus index times size" },
      { id: "b", text: "It searches the block linearly" },
      { id: "c", text: "The CPU caches every element" },
      { id: "d", text: "Elements live in a tree" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 303,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "A hash table trades order for speed",
    body: "A hash table stores key-value pairs in a fixed array and uses a hash function to convert a key into an array index. Lookup, insert, and delete are constant time on average because the function computes the bucket directly instead of searching. Two different keys can collide into the same bucket, and the table resolves that either by chaining, where each bucket holds a small list, or by probing, where the table searches nearby slots. The hash function must be fast and spread keys evenly, otherwise the table degrades toward linear search. The table resizes when it gets too full, rehashing every key into a larger array, which is why a table pays a rare, expensive cost after many cheap operations. What a hash table does not provide is order, so iterating over keys in sorted order is not one of its strengths.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 304,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "How does a hash table handle two keys in one bucket?",
    options: [
      { id: "a", text: "Chaining or probing resolves it" },
      { id: "b", text: "It deletes both keys" },
      { id: "c", text: "It rejects the second key" },
      { id: "d", text: "It sorts the whole table" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 305,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "A binary search tree keeps data sorted",
    body: "A binary search tree is a node-based structure where each node holds a key and points to a left and a right subtree. The ordering invariant is that every key in the left subtree is smaller, and every key in the right subtree is larger, than the node's own key. That invariant makes search, insert, and delete follow a single path from the root, halving the remaining candidates at each step, so each operation is proportional to the tree height. In a balanced tree the height is logarithmic in the number of nodes, which gives the expected logarithmic cost. The catch is that the invariant alone does not guarantee balance: inserting keys in sorted order builds a degenerate tree that looks like a linked list and degrades to linear time. Self-balancing variants, such as AVL and red-black trees, add rotations after each insert and delete to keep the height bounded.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 306,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "What ordering does a binary search tree maintain?",
    options: [
      { id: "a", text: "Left keys smaller, right keys larger" },
      { id: "b", text: "All keys are equal" },
      { id: "c", text: "Left keys larger, right keys smaller" },
      { id: "d", text: "Order by insertion time" },
    ],
    correct_option_id: "a",
  },
];

const systemDesign = [
  {
    card_id: 401,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "A load balancer spreads requests",
    body: "When one server cannot handle the traffic, the answer is to run several and spread requests across them. A load balancer sits in front of the pool and decides which server gets each request. The simplest policies are round robin, which cycles through the pool in order, and least connections, which sends work to the least busy server. The balancer also hides the pool behind one address, so clients do not need to know how many servers exist or when one is added or removed. To keep sessions working, many systems route a client to the same server using a hash of the client identity, or they move session state to shared storage so any server can serve any request. A good balancer also notices when a server is unhealthy and stops sending it traffic, which is how a failing instance becomes invisible to users.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 402,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "How does a load balancer hide the server pool?",
    options: [
      { id: "a", text: "It fronts the pool with one address" },
      { id: "b", text: "It exposes each server's ip" },
      { id: "c", text: "It runs a single server" },
      { id: "d", text: "It caches all requests locally" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 403,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "A cache sits between work and repeat",
    body: "Some work only needs to happen once, and a cache is a fast store that remembers the result so repeats are cheap. The cache is checked before the expensive operation runs. On a hit the stored value is returned immediately. On a miss the slow work runs, the result is stored, and the request completes. The win depends entirely on the hit rate, which is why caches are tuned to the access pattern: hot items, the ones requested again and again, should stay resident. Because a cache holds a limited amount, it needs an eviction policy to decide what to drop when full, with least recently used being the common choice. The hard part is consistency, when the underlying data changes, cached copies can go stale, so caches set time to live values or invalidate entries on write. A cache does not make a system correct, it makes it fast.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 404,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "What determines how much a cache helps?",
    options: [
      { id: "a", text: "The hit rate" },
      { id: "b", text: "The number of evictions" },
      { id: "c", text: "The cache size alone" },
      { id: "d", text: "The number of servers" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 405,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "Indexes make reads fast and writes cost more",
    body: "Finding a row by scanning the whole table is linear in the table size, which gets expensive quickly. An index is a separate structure, typically a B-tree, that maps a column value to the location of matching rows, so lookups follow a short path instead of scanning everything. This makes point reads and range queries dramatically faster. The tradeoff is on writes: every insert, update, and delete must also keep every index on the table consistent, so each write does more work than it would without indexes. That is why the choice of which columns to index matters, indexing the columns you filter and join on helps reads, but indexing everything makes writes slow and bloats storage. Database designers tune this balance per workload, trading a bounded write cost for a large read win.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 406,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "What is the tradeoff of adding an index?",
    options: [
      { id: "a", text: "Faster reads, costlier writes" },
      { id: "b", text: "Faster writes, costlier reads" },
      { id: "c", text: "No effect on writes" },
      { id: "d", text: "Always smaller storage" },
    ],
    correct_option_id: "a",
  },
];

const databases = [
  {
    card_id: 501,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "An index speeds up lookups",
    body: "A table stores rows, and without any index a query that filters on a column must scan every row to find matches. An index on that column is a secondary structure that maps column values to the rows that contain them, so the query jumps straight to the matches instead of scanning. The most common structure is a B-tree, which stays balanced and keeps its height logarithmic in the number of entries, so each lookup touches only a few nodes even in a huge table. Updates are the cost: the database must keep every index in sync on each insert, update, or delete, so each write does extra work per index. The practical rule is to index the columns you filter, join, or sort on, and to avoid indexing columns that only get written, since that adds write cost without helping any read.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 502,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "What is the most common index structure?",
    options: [
      { id: "a", text: "A B-tree" },
      { id: "b", text: "A linked list" },
      { id: "c", text: "A flat file" },
      { id: "d", text: "A hash map only" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 503,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "A transaction groups writes atomically",
    body: "A single logical operation often touches several rows, and a crash in the middle would leave the data half-changed. A transaction groups those writes so they either all commit or all roll back, and the database guarantees there is no in-between state. The implementation keeps a log of changes, and committing appends a commit record to it. If the system crashes before that record exists, recovery rolls the work back; if the record exists, the work is redone or left as committed. Grouping writes this way lets an application express multi-step operations such as moving money between two accounts without ever exposing a state where the money has left one account but not arrived at the other. The application signals the boundary by beginning a transaction, doing the writes, and then committing or aborting at the end.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 504,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "What decides whether a crash rolls a transaction back?",
    options: [
      { id: "a", text: "Whether a commit record exists" },
      { id: "b", text: "How many rows it touched" },
      { id: "c", text: "Whether the client retries" },
      { id: "d", text: "The age of the transaction" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 505,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "ACID describes transaction guarantees",
    body: "ACID is the set of guarantees a transactional database offers, and each letter names one. Atomicity means a transaction's writes all happen or none do, with no partial result. Consistency means a transaction moves the database from one valid state to another, preserving the rules the schema and application define. Isolation means concurrent transactions do not see each other's uncommitted work, and the database serializes them as if each ran alone, though the degree of isolation is configurable. Durability means once a transaction commits, its effects survive a crash, because the commit record is written to durable storage before the transaction is acknowledged. Together these guarantees let an application treat a group of operations as one reliable unit. Weakening isolation improves concurrency and throughput, which is why many systems trade a strict setting for a faster one.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 506,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "What does durability guarantee?",
    options: [
      { id: "a", text: "Committed changes survive a crash" },
      { id: "b", text: "All transactions run at once" },
      { id: "c", text: "Reads never see committed writes" },
      { id: "d", text: "The database never locks" },
    ],
    correct_option_id: "a",
  },
];

const networkProtocols = [
  {
    card_id: 701,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "Layers split the network into responsibilities",
    body: "The network stack is divided into layers so each protocol only worries about its own job. A layer receives data from the layer above, adds the header it needs, and hands the result to the layer below. The most common model has five layers: application, transport, network, link, and physical. Each one solves a different problem, so a protocol can change inside one layer without rewriting the others. HTTP works the same over Ethernet, Wi-Fi, or cellular because it sits above the transport layer and never cares which link carries it. When a packet arrives, each layer strips the header that belongs to it and passes the payload upward, so the application sees exactly the data the remote application sent. This separation is why the internet can evolve, new layers can be added or replaced while the ones above and below keep working unchanged.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 702,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "Why does a layer add a header before sending down?",
    options: [
      { id: "a", text: "It carries control data for its own layer" },
      { id: "b", text: "It makes the packet heavier" },
      { id: "c", text: "It hides the payload forever" },
      { id: "d", text: "It encrypts the whole packet" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 703,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "Ethernet frames travel on one link",
    body: "On a single physical link, data moves in Ethernet frames. A frame wraps the network packet with a header and a trailer: the header carries the destination and source MAC addresses, which identify the network interfaces on that link, and the trailer holds a checksum the receiver uses to detect corruption. An Ethernet switch reads the destination MAC address and forwards the frame only out the port where that address lives, learned by watching which addresses appear on which ports. This is how a frame gets from one machine to another on the same network without copying it everywhere. The frame does not know or care about IP addresses, it only cares about the two interfaces directly connected to that one link. Frames are the unit of travel within a single network segment, and every IP packet ends up inside one or more of them on its way across the internet.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 704,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "What does an Ethernet switch use to forward a frame?",
    options: [
      { id: "a", text: "The destination MAC address" },
      { id: "b", text: "The destination IP address" },
      { id: "c", text: "The application port" },
      { id: "d", text: "The frame checksum" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 705,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "IP addresses reach machines across networks",
    body: "The network layer moves packets between machines that may sit on different networks, and it does that with IP addresses. An IP address is a stable identifier for a machine's network interface, independent of the hardware address it uses on a local link. Every packet carries a source and a destination IP address, and routers use the destination to decide the next hop. A router does not need to know the full path, it only needs to know which neighbor is closer to the destination and forwards the packet there. To find the hardware address for a machine on the local network, a host uses ARP, the Address Resolution Protocol, which broadcasts a query and learns the MAC address of the IP it wants. Because IP addresses are what routers understand, they are what makes a packet survive travel across many different link types between the two end machines.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 706,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "How does a host find the MAC address for a local IP?",
    options: [
      { id: "a", text: "It uses ARP to resolve it" },
      { id: "b", text: "It asks the remote router" },
      { id: "c", text: "It reads the DNS cache" },
      { id: "d", text: "It guesses from the IP" },
    ],
    correct_option_id: "a",
  },
];

const networkSecurity = [
  {
    card_id: 601,
    order_index: 0,
    type: "concept",
    template: "text_only",
    title: "Symmetric encryption keeps a shared secret",
    body: "Symmetric encryption protects data with a single secret key that both the sender and the receiver hold. The sender runs the key and the plaintext through a cipher, producing ciphertext that reveals nothing without the key, and the receiver runs the same key in reverse to recover the plaintext. Because the same key does both jobs, the algorithms are fast and well suited to bulk data. The difficulty is not the cipher, it is the key itself: the two sides must share the secret ahead of time over a channel that keeps it private, and if that key leaks, everything encrypted with it leaks too. This is why symmetric encryption appears inside larger protocols, wrapped by a scheme that solves the key-sharing problem, rather than standing alone.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 602,
    order_index: 1,
    type: "quiz",
    tests_card_id: 0,
    question: "What is the main challenge of symmetric encryption?",
    options: [
      { id: "a", text: "Sharing the secret key securely" },
      { id: "b", text: "The cipher is too fast" },
      { id: "c", text: "It reveals the plaintext" },
      { id: "d", text: "It needs no key at all" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 603,
    order_index: 2,
    type: "concept",
    template: "text_only",
    title: "Asymmetric encryption uses a key pair",
    body: "Asymmetric encryption, also called public-key encryption, replaces the single shared secret with a pair of keys that work together. One key is public and can be given to anyone, the other is private and stays with its owner. A message encrypted with the public key can only be decrypted with the private key, so anyone can send a message to the owner without ever sharing a secret. This solves the key-distribution problem that symmetric encryption has, but it costs speed, public-key operations are far slower than symmetric ones. Real protocols combine the two: they use the slow asymmetric step just long enough to agree on a fresh symmetric key, then switch to the fast cipher for the actual data.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 604,
    order_index: 3,
    type: "quiz",
    tests_card_id: 2,
    question: "How do real protocols combine both cipher types?",
    options: [
      { id: "a", text: "Asymmetric to agree on a symmetric key" },
      { id: "b", text: "Symmetric to protect the key pair" },
      { id: "c", text: "They use only one type always" },
      { id: "d", text: "They encrypt keys with plaintext" },
    ],
    correct_option_id: "a",
  },
  {
    card_id: 605,
    order_index: 4,
    type: "concept",
    template: "text_only",
    title: "TLS protects traffic on the wire",
    body: "Transport Layer Security, TLS, is the protocol that keeps traffic private as it crosses the network, the lock icon in a browser address bar. Before any application data flows, the client and server perform a handshake: they negotiate a cipher suite, authenticate the server with a certificate, and derive session keys. The handshake uses asymmetric encryption to verify identity and exchange a secret without eavesdroppers learning it, and from then on all records are encrypted with the fast symmetric keys both sides derived. TLS also authenticates the data with message authentication codes, so a receiver can detect if anything was altered in transit. This is why HTTPS, HTTP over TLS, protects both the confidentiality and the integrity of everything a user sends and receives.",
    code_snippet: null,
    diagram_ref: null,
  },
  {
    card_id: 606,
    order_index: 5,
    type: "quiz",
    tests_card_id: 4,
    question: "What does the TLS handshake establish before data flows?",
    options: [
      { id: "a", text: "Cipher suite, identity, and session keys" },
      { id: "b", text: "The plaintext of every message" },
      { id: "c", text: "The user's password" },
      { id: "d", text: "Nothing, data flows immediately" },
    ],
    correct_option_id: "a",
  },
];

export const deckStore = {
  "operating-systems": placeholderDeck.cards,
  "computer-networks": computerNetworks,
  "data-structures": dataStructures,
  "system-design": systemDesign,
  databases,
  "network-security": networkSecurity,
  "network-protocols": networkProtocols,
};
